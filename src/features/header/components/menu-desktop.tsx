import { useNavigationContext, PageRefs } from '@/context';import { headerConfig } from '../config/header-config';export const MenuDesktop = () => {    const { navigateTo } = useNavigationContext();    return (        <div className="box-border hidden size-full items-center justify-end lg:flex">            <ul className={`flex flex-row items-center gap-8`}>                {headerConfig.navLinks.map((link) => {                    return (                        <li                            key={link.id}                            onClick={() =>                                navigateTo(link.id as keyof PageRefs)                            }                        >                            {link.isButton ? (                                <h3                                    className={                                        'font-subHeadline text-white hover:cursor-pointer'                                    }                                >                                    {link.label}                                </h3>                            ) : (                                <h3                                    className={                                        'font-subHeadline text-white hover:cursor-pointer'                                    }                                >                                    {link.label}                                </h3>                            )}                        </li>                    );                })}            </ul>        </div>    );};