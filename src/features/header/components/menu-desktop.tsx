import * as React from 'react';import { headerConfig } from '../config/header-config.tsx';export const MenuDesktop = () => {    return (        <div className="box-border hidden size-full items-center justify-end lg:flex">            <ul className={`flex flex-row items-center gap-8`}>                {headerConfig.navLinks.map((link) => {                    return (                        <li key={link.id}>                            {link.isButton ? (                                <h3                                    className={'font-subHeadline text-gray-500'}                                >                                    {link.label}                                </h3>                            ) : (                                <h3                                    className={'font-subHeadline text-gray-500'}                                >                                    {link.label}                                </h3>                            )}                        </li>                    );                })}            </ul>        </div>    );};