import * as React from 'react';import { ScrollingCarousel } from '@/features/scrolling-text/scrolling-carousel';type SplitHeadingProps = {    children: string;};const Heading = ({ children }: SplitHeadingProps) => {    return (        <h1            className={`text-center font-headline text-9xl font-light uppercase leading-[300px] text-primary-grey/20 2xl:text-[300px] 2xl:leading-[400px]`}        >            {children}        </h1>    );};const SplitHeading: React.FC<SplitHeadingProps> = ({ children }) => {    return (        <div className={`flex h-fit flex-row 2xl:mt-32`}>            {children.split('').map((item, idx) =>                item === ' ' ? (                    <h1                        key={idx}                        className={`text-9xl font-light uppercase text-primary-grey/20 2xl:text-[300px] 2xl:leading-[400px]`}                    >                        &nbsp;                    </h1>                ) : (                    <Heading key={idx}>{item}</Heading>                ),            )}            <h1                className={`text-9xl font-light uppercase text-primary-grey/20 2xl:text-[300px] 2xl:leading-[400px]`}            >                &nbsp;            </h1>        </div>    );};export function ScrollingText() {    return (        <ScrollingCarousel>            <SplitHeading>                MIDDLE BEACH INDUSTRIAL ESTATE • MIDDLE BEACH INDUSTRIAL ESTATE                •            </SplitHeading>        </ScrollingCarousel>    );}