import React from 'react';import placeholder from '@/assets/availability-placeholder.png';import { Section } from '@/components/section';import { SectionSubtitle, SectionTitle } from '@/components/text';import { useNavigationContext } from '@/context';export function Availability() {    const { pageRefs } = useNavigationContext();    return (        <Section ref={pageRefs.availability}>            <div className={'flex flex-col gap-16 py-32'}>                <SectionTitle>AVAILABILITY</SectionTitle>                <div className={'flex size-full items-center justify-center'}>                    <img                        src={placeholder}                        alt="placeholder"                        className={'object-cover'}                    />                </div>            </div>        </Section>    );}