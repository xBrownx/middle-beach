import React from 'react';import { Section } from '@/components/section';import { SectionSubtitle, SectionTitle } from '@/components/text';import { useNavigationContext } from '@/context';export function Availability() {    const { pageRefs } = useNavigationContext();    return (        <Section ref={pageRefs.availability}>            <div className={'flex flex-col gap-8'}>                <SectionTitle>AVAILABILITY</SectionTitle>                <SectionSubtitle>                    3341A Port Wakefield Highway, Middle Beach SA 5501                </SectionSubtitle>                <div                    className={                        'flex aspect-video size-full items-center justify-center bg-primary-grey'                    }                >                    <p className={'font-body text-4xl'}>IN WORK</p>                </div>            </div>        </Section>    );}