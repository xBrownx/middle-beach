import React, { useState } from 'react';import { Section } from '@/components/section';import { SectionBody, SectionSubtitle, SectionTitle } from '@/components/text';import { useNavigationContext } from '@/context';import { LOT_DATA_11_20, LOT_DATA_2_10 } from '@/sections/availability/data';import { AvailabilityMap } from '@/sections/availability/map';import { Lot } from '@/sections/availability/types';import { cn } from '@/utils/cn';export function Availability() {    const { pageRefs } = useNavigationContext();    const [hoverId, setHoverId] = useState<string | null>(null);    return (        <Section ref={pageRefs.availability}>            <div                className={                    'relative box-border flex size-full flex-col gap-4 lg:gap-4 lg:py-8 2xl:py-0'                }            >                <div className={'flex w-full flex-col lg:flex-row'}>                    <div className={'flex-1'}>                        <SectionTitle>AVAILABILITY</SectionTitle>                    </div>                    <div                        className={                            'relative hidden flex-1 flex-row items-center gap-4 lg:flex'                        }                    >                        <SectionSubtitle>SITE PLAN</SectionSubtitle>                        <AvailableOrSold className={'hidden lg:flex'} />                    </div>                </div>                <div                    className={                        'flex size-full flex-col gap-8 2xl:flex-row 2xl:gap-0'                    }                >                    <div className={'order-2 flex flex-1 lg:order-1'}>                        <LotButtons hoverId={hoverId} setHoverId={setHoverId} />                    </div>                    <div className={'order-1 size-full flex-1 lg:order-2'}>                        <SectionSubtitle className={'lg:hidden'}>                            SITE PLAN                        </SectionSubtitle>                        <AvailabilityMap                            hoverId={hoverId}                            setHoverId={setHoverId}                        />                        <div className={'mt-4 lg:hidden'}>                            <AvailableOrSold />                        </div>                    </div>                </div>            </div>        </Section>    );}function LotButtons({    hoverId,    setHoverId,}: {    hoverId: string | null;    setHoverId: (hoverId: string | null) => void;}) {    return (        <div            className={cn(                'size-full flex flex-row gap-8 lg:gap-16 lg:pl-4 lg:pr-16 pt-4 lg:pt-0',            )}        >            <div className={'flex size-full flex-col  gap-2'}>                <ul className={'flex h-full flex-col justify-end gap-3'}>                    <li                        className={                            'flex h-10 flex-row items-center justify-center gap-5 rounded-[8px] bg-primary-grey'                        }                    ></li>                    {LOT_DATA_2_10.map((lot) => (                        <LotButton                            key={lot.id}                            lot={lot}                            hoverId={hoverId}                            setHoverId={setHoverId}                        />                    ))}                </ul>            </div>            <div className={'flex size-full flex-col justify-end gap-2'}>                <ul className={'flex flex-col gap-3'}>                    {LOT_DATA_11_20.map((lot) => (                        <LotButton                            key={lot.id}                            lot={lot}                            hoverId={hoverId}                            setHoverId={setHoverId}                        />                    ))}                </ul>            </div>        </div>    );}function LotButton({    lot,    hoverId,    setHoverId,}: {    lot: Lot;    hoverId: string | null;    setHoverId: (hoverId: string | null) => void;}) {    return (        <li            className={cn(                'flex h-11 flex-row items-center justify-center gap-2 lg:gap-5 rounded-[8px]  transition-all duration-300 hover:cursor-pointer hover:bg-primary-indigo',                hoverId === lot.id ? 'bg-primary-indigo' : 'bg-secondary-blue',            )}            onMouseEnter={() => setHoverId(lot.id)}            onMouseLeave={() => setHoverId(null)}        >            <SectionSubtitle                className={'flex-1 text-end text-sm text-white md:text-xl'}            >                {lot.label}            </SectionSubtitle>            <SectionSubtitle className={'text-center text-white'}>                |            </SectionSubtitle>            <SectionBody className={'flex-1 text-xs text-white lg:text-lg'}>                {lot.area}            </SectionBody>        </li>    );}function AvailableOrSold({ className }: { className: string }) {    return (        <div            className={cn(                'absolute flex w-full flex-row items-center justify-center gap-8',                className,            )}        >            <SectionBody                className={'border-l-[24px] border-secondary-blue pl-2'}            >                AVAILABLE            </SectionBody>            <SectionBody className={'border-l-[24px] border-primary-grey pl-2'}>                SOLD            </SectionBody>        </div>    );}