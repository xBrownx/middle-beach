import React, { useState } from 'react';import { Section } from '@/components/section';import { SectionBody, SectionSubtitle, SectionTitle } from '@/components/text';import { useNavigationContext } from '@/context';import { LOT_DATA_12_21, LOT_DATA_2_11 } from '@/sections/availability/data';import { AvailabilityMap } from '@/sections/availability/map';import { Lot } from '@/sections/availability/types';import { cn } from '@/utils/cn';export function Availability() {    const { pageRefs } = useNavigationContext();    const [hoverId, setHoverId] = useState<string | null>(null);    return (        <Section ref={pageRefs.availability} className={'lg:px-8 lg:py-32'}>            <div                className={                    'relative box-border flex size-full flex-col gap-4 xl:py-8 2xl:gap-4 2xl:py-0'                }            >                <div className={'flex w-full flex-col lg:flex-row'}>                    <div className={'flex-1'}>                        <SectionTitle>AVAILABILITY</SectionTitle>                    </div>                    <div                        className={                            'relative hidden flex-1 flex-row items-center gap-4 2xl:flex'                        }                    >                        <SectionSubtitle>SITE PLAN</SectionSubtitle>                        <AvailableOrSold className={'hidden 2xl:flex'} />                    </div>                </div>                <div                    className={                        'flex size-full flex-col gap-8 2xl:flex-row 2xl:gap-0'                    }                >                    <div className={'order-2 flex flex-1 2xl:order-1'}>                        <LotButtons hoverId={hoverId} setHoverId={setHoverId} />                    </div>                    <div className={'order-1 size-full flex-1 2xl:order-2'}>                        <SectionSubtitle className={'2xl:hidden'}>                            SITE PLAN                        </SectionSubtitle>                        <AvailabilityMap                            hoverId={hoverId}                            setHoverId={setHoverId}                        />                        <div className={'mt-4 2xl:hidden'}>                            <AvailableOrSold />                        </div>                    </div>                </div>            </div>        </Section>    );}function LotButtons({    hoverId,    setHoverId,}: {    hoverId: string | null;    setHoverId: (hoverId: string | null) => void;}) {    return (        <div            className={cn(                'size-full flex flex-row gap-8 2xl:gap-16 2xl:pl-4 2xl:pr-16 pt-4 2xl:pt-0',            )}        >            <div className={'flex size-full flex-col  gap-2'}>                <ul className={'flex h-full flex-col justify-end gap-3'}>                    {LOT_DATA_2_11.map((lot) => (                        <LotButton                            key={lot.id}                            lot={lot}                            hoverId={hoverId}                            setHoverId={setHoverId}                        />                    ))}                </ul>            </div>            <div className={'flex size-full flex-col justify-end gap-2'}>                <ul className={'flex flex-col gap-3'}>                    {LOT_DATA_12_21.map((lot) => (                        <LotButton                            key={lot.id}                            lot={lot}                            hoverId={hoverId}                            setHoverId={setHoverId}                        />                    ))}                </ul>            </div>        </div>    );}function LotButton({    lot,    hoverId,    setHoverId,}: {    lot: Lot;    hoverId: string | null;    setHoverId: (hoverId: string | null) => void;}) {    return (        <li            className={cn(                'flex h-11 flex-row items-center justify-center gap-2 lg:gap-5 rounded-[8px]  transition-all duration-300 hover:cursor-pointer hover:bg-primary-indigo',                hoverId === lot.id ? 'bg-primary-indigo' : 'bg-secondary-blue',            )}            onMouseEnter={() => setHoverId(lot.id)}            onMouseLeave={() => setHoverId(null)}        >            <SectionSubtitle                className={'flex-1 text-end text-sm text-white md:text-xl'}            >                {lot.label}            </SectionSubtitle>            <SectionSubtitle className={'text-center text-white'}>                |            </SectionSubtitle>            <SectionBody className={'flex-1 text-xs text-white lg:text-lg'}>                {lot.area}            </SectionBody>        </li>    );}function AvailableOrSold({ className }: { className?: string }) {    return (        <div            className={cn(                'absolute flex w-full flex-row items-center justify-center gap-8',                className,            )}        >            <SectionBody                className={'border-l-[24px] border-secondary-blue pl-2'}            >                AVAILABLE            </SectionBody>            <SectionBody className={'border-l-[24px] border-primary-grey pl-2'}>                SOLD            </SectionBody>        </div>    );}