import video from '@/assets/location-vid-placeholder.mp4';import { Section } from '@/components/section';import { SectionSubtitle, SectionTitle } from '@/components/text';import { Video } from '@/components/video';import { useNavigationContext } from '@/context';export function Location() {    const { pageRefs } = useNavigationContext();    return (        <Section            ref={pageRefs.location}            className={'bg-primary-indigo lg:py-32'}        >            <div className={'flex flex-col gap-8'}>                <SectionTitle className={'text-primary-white'}>                    LOCATION                </SectionTitle>                <SectionSubtitle className={'text-primary-white'}>                    3341A Port Wakefield Highway, Middle Beach SA 5501                </SectionSubtitle>                <Video src={video} />            </div>        </Section>    );}