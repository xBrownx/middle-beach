import placeholder from '@/assets/landing-placeholder.jpg';import { useNavigationContext } from '@/context';import { DownArrow } from '@/sections/landing/components/down-arrow';export function Landing() {    const { pageRefs } = useNavigationContext();    return (        <section            className={'size-full max-h-screen overflow-hidden'}            ref={pageRefs.landing}        >            <img                className={'size-full object-cover'}                src={placeholder}                alt="gallery"            />            <DownArrow />        </section>    );}