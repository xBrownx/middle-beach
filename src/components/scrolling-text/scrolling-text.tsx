import * as React from 'react';import { ScrollingCarousel } from '@/components/scrolling-text/scrolling-carousel';type SplitHeadingProps = {    children: string;};const SplitHeading: React.FC<SplitHeadingProps> = ({ children }) => {    return (        <div className={`flex flex-row 2xl:mt-32`}>            {children.split('').map((item, idx) =>                item === ' ' ? (                    <h1                        key={idx}                        className={`text-[200px] font-light uppercase leading-[300px] text-primary-grey/20 2xl:text-[300px] 2xl:leading-[400px]`}                    >                        &nbsp;                    </h1>                ) : (                    <h1                        key={idx}                        className={`text-center font-headline text-[200px] font-light uppercase leading-[300px] text-primary-grey/20 2xl:text-[300px] 2xl:leading-[400px]`}                    >                        {item}                    </h1>                ),            )}            <h1                className={`text-[300px] font-light uppercase leading-[300px] text-primary-grey/20`}            >                &nbsp;            </h1>        </div>    );};export function ScrollingText() {    return (        <ScrollingCarousel>            <SplitHeading>                MIDDLE BEACH INDUSTRIAL ESTATE • MIDDLE BEACH INDUSTRIAL ESTATE                •            </SplitHeading>        </ScrollingCarousel>    );}