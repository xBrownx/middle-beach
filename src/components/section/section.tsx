import * as React from 'react';import { forwardRef, PropsWithChildren, StyleHTMLAttributes } from 'react';import { cn } from '@/utils/cn';export const Section = forwardRef<    HTMLElement,    PropsWithChildren & StyleHTMLAttributes<HTMLElement>>(function Section({ className, children }, ref) {    return (        <section            className={cn(                'flex 2xl:min-h-screen w-screen items-center justify-center ',                className,            )}            ref={ref}        >            <div                className={cn(                    'relative w-full max-w-[1440px] box-border',                    '2xl:p0',                    'px-4 py-16',                )}            >                {children}            </div>        </section>    );});