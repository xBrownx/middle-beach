import * as React from 'react';import { PropsWithChildren } from 'react';import { cn } from '@/utils/cn';export function SectionTitle({    className,    children,}: PropsWithChildren & React.StyleHTMLAttributes<HTMLElement>) {    return (        <h1            className={cn(                'm-0  p-0  font-headingVariant font-bold md:text-8xl text-font-primary',                'text-5xl pl-4',                className,                //border-l-[32px] border-primary-red pl-4            )}        >            {children}        </h1>    );}